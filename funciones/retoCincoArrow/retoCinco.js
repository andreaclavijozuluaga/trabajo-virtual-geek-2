const parametros = (number) => {
    return (number + number + number + number)*2;
};
parametros(4,5,3,7)*2;

const parametros = function (number) {
    return (number + number + number + number)*2;
};

